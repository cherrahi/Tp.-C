#include <stdio.h>
#include <stdlib.h>
#include <string.h>
char *ChargerChaine(int N) {
    char *chaine = (char *)malloc(N + 1); 
    if (chaine == NULL) {
        printf("erreur d'allocation de mémoire \n");
       return 1;
    }
    fgets(chaine, N + 1, stdin);
    chaine[strcspn(chaine, "\n")] = '\0';
    return chaine;
}
int Longueur(char *ch) {
    int i=0;
while(ch[i]!='\0'){
i++;{
return i;
}
void ChargerTab(char *p, char Tab[]) {
    int i=0;
  while(p[i]!='\0'){
Tab[i]=p[i]; 
i++; 
{Tab[i]='\0'; 
}
void InverserTab(char Tab[], char T[], int m) {
    for (int i = 0; i < m; i++) {
        T[i] = Tab[m - i - 1];
    }
}
void AfficherTab(char Tab[], int m) {
    for (int i = 0; i < m; i++) {
        printf("%c", Tab[i]);
    }
    printf("\n");
}
int main() {
    int N;
    printf("entrez la longueur de la chaine: ");
    scanf("%d", &N);
    char *chaine = ChargerChaine(N);
    int longueur = Longueur(chaine);
    char Tab[longueur];
    ChargerTab(chaine, Tab);
    char T[longueur];
    InverserTab(Tab, T, longueur);
    printf("la fonction originale: ");
    AfficherTab(Tab, longueur);
    printf("serie inversee: ");
    AfficherTab(T, longueur);
    free(chaine);
    return 0;
}
